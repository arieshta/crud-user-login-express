const express = require('express');
const { logoutController } = require('../controllers/logInOutControllers');

const router = express.Router();

// Logout user
// router.get('/', logoutController);

module.exports = router;
